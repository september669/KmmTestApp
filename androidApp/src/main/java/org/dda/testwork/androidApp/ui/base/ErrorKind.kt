package org.dda.testwork.androidApp.ui.base

/*
fun Throwable.toErrorKind(): ErrorKind {
    return when (this) {
        is ApiException -> if (code() == 502 || code() == 503 || code() == 504) {
            ErrorKind.Maintenance
        } else if (code() == 404) {
            ErrorKind.NotFound
        } else {
            ErrorKind.Api(code(), apiErrorCode)
        }
        is IOException -> ErrorKind.Network
        else -> ErrorKind.Generic
    }
}*/
